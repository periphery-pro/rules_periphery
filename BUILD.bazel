# This package is loaded whenever the toolchain type is resolved, so it must
# not reference dev dependencies; those live in //dev.

exports_files([
    "defs.bzl",
    "extensions.bzl",
    "rules.bzl",
])

toolchain_type(
    name = "toolchain_type",
    visibility = ["//visibility:public"],
)
